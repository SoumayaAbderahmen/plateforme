{% extends 'base.html.twig' %}

{% block title %}Offre index{% endblock %}
{% block js %}
	<!-- Theme JS files -->
	<script src="{{ asset('global_assets/js/plugins/tables/datatables/datatables.min.js') }}"></script>
	<script src="{{ asset('global_assets/js/plugins/forms/selects/select2.min.js') }}"></script>

	<script src="{{ asset('assets/js/app.js') }}"></script>
	<script src="{{ asset('global_assets/js/demo_pages/datatables_basic.js') }}"></script>
{% endblock %}
{% block body %}
<div class="content">
   <div class="card-header header-elements-inline" style="background-color:RoyalBlue">
						<h3 class="card-title" style="color:white">Offre</h3>
						<div class="header-elements">
							<a class="btn btn-primary" href="{{ path('offre_new') }}" style="width:100px" align="right" ><i class="icon-add"></i> Ajouter</a>
	                	</div>
					</div>
	
    

    <table class="table datatable-basic dataTable no-footer" id="DataTables_Table_0" role="grid" aria-describedby="DataTables_Table_0_info">
        <thead>
            <tr>
            <th>Id</th>
            <th>Description</th>
            <th>Grille Tarifaire</th>
            <th>Date Dèbut</th>
            <th>Date Fin</th>
            <th>Pays</th>    
            <th>actions</th>
           
           
            </tr>
        </thead>
        <tbody>
        
        {% for offre in offres %}
            <tr>
             <td>{{ offre.id }}</td>
             <td>{{offre.description}}</td>
                {% for o in offre.grilletarifaires %}
               <td>{{ o.description }}</td>
               <td>{{o.datedebut|date('Y/m/d')}}</td>
               <td>{{o.datefin|date('Y/m/d')}}</td>
              {% endfor %}
            {% for o in offre.pays %}
            
                <td>{{ o.nom}}</td>
               
            
            {% endfor %}
               <td>
                    <a  class="btn btn-primary" href="{{ path('offre_edit', {'id': offre.id}) }}"style="width:100px"><i class="icon-pencil"></i> edit</a>
                 {{ include('offre/_delete_form.html.twig') }}
                </td>
               </tr>
            {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
   
   
   
    </div>
{% endblock %}
