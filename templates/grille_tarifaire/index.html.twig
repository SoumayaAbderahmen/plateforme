{% extends 'base.html.twig' %}

{% block title %}GrilleTarifaire index{% endblock %}
{% block js %}
	<!-- Theme JS files -->
	<script src="{{ asset('global_assets/js/plugins/tables/datatables/datatables.min.js') }}"></script>
	<script src="{{ asset('global_assets/js/plugins/forms/selects/select2.min.js') }}"></script>

	<script src="{{ asset('assets/js/app.js') }}"></script>
	<script src="{{ asset('global_assets/js/demo_pages/datatables_basic.js') }}"></script>
{% endblock %}
{% block body %}
<div class="content">
   <div class="card-header header-elements-inline" style="background-color:RoyalBlue">
						<h3 class="card-title" style="color:white">Grille Tarifaire</h3>
						<div class="header-elements">
							<a class="btn btn-primary" href="{{ path('grille_tarifaire_new') }}" style="width:100px" align="right" ><i class="icon-add"></i> Ajouter</a>
	                	</div>
					</div>
   

    <table class="table datatable-basic dataTable no-footer" id="DataTables_Table_0" role="grid" aria-describedby="DataTables_Table_0_info">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date début</th>
                <th>Date fin</th>
                <th>Description</th>
               <th>Prix</th>

                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for grille_tarifaire in grille_tarifaires %}
            <tr>
                <td>{{ grille_tarifaire.id }}</td>
                <td>{{ grille_tarifaire.datedebut|date('Y/m/d') }}</td>
                <td>{{ grille_tarifaire.datefin|date('Y/m/d') }}</td>
                <td>{{ grille_tarifaire.description }}</td>
                <td>{{grille_tarifaire.prix}}</td>
               


                <td>
                    <a class="btn btn-primary" href="{{ path('grille_tarifaire_edit', {'id': grille_tarifaire.id}) }}"style="width:100px"><i class="icon-pencil"></i> Modifier</a>
                     {{ include('grille_tarifaire/_delete_form.html.twig') }}               
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

   </div>
{% endblock %}
