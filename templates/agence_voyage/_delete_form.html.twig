<form method="post" action="{{ path('agence_voyage_delete', {'id': agence_voyage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agence_voyage.id) }}">
    <button class="btn btn-primary" style="width:100px;background-color:Red"  ><i class="icon-bin2"></i> Supprimer</button>
</form>
