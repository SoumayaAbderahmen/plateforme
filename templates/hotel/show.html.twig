{% extends 'base.html.twig' %}

{% block title %}Hotel{% endblock %}

{% block body %}
    <h1>Hotel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hotel.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ hotel.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ hotel.description }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ hotel.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a  class="btn btn-primary" style="width:100px" href="{{ path('hotel_index') }}"><i class="icon-undo2"></i> back to list</a>
<br>
    <a class="btn btn-primary" style="width:100px" href="{{ path('hotel_edit', {'id': hotel.id}) }}"><i class="icon-pencil"></i> edit</a>
<br>
    {{ include('hotel/_delete_form.html.twig') }}
{% endblock %}
